互操作性是应用程序与其它应用程序相互通信和协同工作的能力，而这些应用程序不一定是针对同一平台。

COM Interop 服务(CIS)层提供了一种无线和无缝的方式。

将COM组件公开为.NET类的包装类。

这种服务名称为CIS,分为两部分

让COM组件在.NET中使用 使用工具tlbimp.exe
让.NET组件在COM中使用 使用工具tlbexp.exe


在.NET中使用ADO的理由

1.保留在ADO代码中的投入。
2.最小化对.NET的学习曲线。
3.ADO.NET没有100%覆盖ADO的功能。

ADO.NET不支持服务器游标功能。



