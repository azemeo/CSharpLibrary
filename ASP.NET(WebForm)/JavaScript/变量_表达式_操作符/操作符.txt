1.代数操作符

常用:+,-,*,/
取模:%
增量:++,--,A++,++A,A--,--A
取反:-

2.位操作符
与&，或|，异或^，取反~，左移<<，右移>>,填零右移>>>

3.逻辑操作符
与&&,或||,取反!

4.比较操作符
等于(==)，不等于(!=)，大于(>)，小于(<)，大于等于(>=)，小于等于(<=)

5.字符串操作 
+,+=
6.优先级
赋值>条件>逻辑...
括号可以改变优先级




搜索关键字：操作符，Javascript