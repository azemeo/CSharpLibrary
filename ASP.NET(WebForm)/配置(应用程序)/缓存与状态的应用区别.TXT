缓存与状态的应用区别

应用程序缓存:(简称缓冲),Cache,为ASP.NET新增的，生存期在整个应用程序的过程。

常用于：只读信息的放入内存中。(股票的动态显示牌)


应用程序状态:(简称状态),Application,为ASP沿过来的，生存期在整个应用程序的过程。

常用于：读简短信息的放入内存中,在程序结束时(IIS重启、重启计算机、关机)写入文件中。(记数器)

两者都可以通过(IIS重启、重启计算机、关机)使信息丢。